name: ZAP & Trivy Security Scan

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  zap:
    runs-on: ubuntu-latest
    steps:
      # ---- ① 检出源码 ----
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      # ---- ② 安装 Node 与依赖 ----
      - name: Setup Node
        uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0
        with:
          node-version: 24
          cache: npm
      - run: npm ci

      # ---- ③ 构建静态包 ----
      - name: Build
        run: npm run build
        # 产出目录 dist (vite) 或 build (vue-cli)

      # ---- ④ 构建 Docker 镜像 ----
      - name: Build Docker Image
        run: |
          docker build -t ceramicraft-customer-frontend:${{ github.sha }} .
          docker tag ceramicraft-customer-frontend:${{ github.sha }} ceramicraft-customer-frontend:latest

      # ---- ⑤ Trivy 镜像漏洞扫描 ----
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@6e7b7d1fd3e4fef0c5fa8cce1229c54b2c9bd0d8 # 0.24.0
        with:
          image-ref: 'ceramicraft-customer-frontend:latest'
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH'

      - name: Upload Trivy scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@ea9e4e37992a54ee68a9622e985e60c8e8f12d9f # v3.27.4
        with:
          sarif_file: 'trivy-results.sarif'

      # ---- ⑥ 起本地 HTTP 服务器 ----
      - name: Serve site for ZAP
        run: |
          # 启动 HTTP 服务器（这里用 vite 自带的）
          npx serve dist -l 4173 &
          # 等待监听成功
          timeout 60 bash -c 'until nc -z 127.0.0.1 4173; do sleep 1; done'

      # ---- ⑦ ZAP Full Scan ----
      - name: Run OWASP ZAP Full Scan
        uses: zaproxy/action-full-scan@3c58388149901b9a03b7718852c5ba889646c27c # v0.12.0
        with:
          target: 'http://127.0.0.1:4173'
          rules_file_name: '.zap/rules.tsv'   # 可选，屏蔽误报
          fail_action: false                  # 有漏洞也不让 CI 失败
          allow_issue_writing: false           # 关闭自动开 GitHub Issue
          issue_title: 'ZAP 安全扫描告警'
          artifact_name: 'zap-report'         # 附件报告
